services:

  backend:
    build: ./backend

    ports:
      - "8000:8000"

  frontend:
    build: ./frontend

    ports:
      - "3000:3000"

    environment:
      - HOST=0.0.0.0

    stdin_open: true
    tty: true

    depends_on:
      - backend